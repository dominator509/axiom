[package]
name = "axiom-rand-regression-baseline"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[features]
entropy-fault-injection = []

[lib]
path = "../src/lib.rs"

[dependencies]
rand08 = { package = "rand", version = "=0.8.5", features = ["log"] }
rand10 = { package = "rand", version = "=0.10.0", features = ["log"] }
log = "=0.4.32"
rand_chacha = "=0.3.1"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "=0.2.189"
