diff --git a/dist/types/icns.js b/dist/types/icns.js
index f2bfafef3723cb423b110304815e56e3f97f81e0..b51c380e9f33f00a25e26bd262e58f9d659be753 100644
--- a/dist/types/icns.js
+++ b/dist/types/icns.js
@@ -64,10 +64,20 @@ const ICON_TYPE_SIZE = {
     ic10: 1024,
 };
 function readImageHeader(input, imageOffset) {
+    if (input.length - imageOffset < SIZE_HEADER) {
+        throw new TypeError('Invalid ICNS entry header');
+    }
     const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
+    const imageLength = (0, utils_1.readUInt32BE)(input, imageLengthOffset);
+    // Every ICNS entry includes an 8-byte type/length header. A zero or
+    // undersized length leaves imageOffset unchanged and previously caused an
+    // infinite loop (CVE-2025-71330).
+    if (imageLength < SIZE_HEADER || imageOffset + imageLength > input.length) {
+        throw new TypeError('Invalid ICNS entry length');
+    }
     return [
         (0, utils_1.toUTF8String)(input, imageOffset, imageLengthOffset),
-        (0, utils_1.readUInt32BE)(input, imageLengthOffset),
+        imageLength,
     ];
 }
 function getImageSize(type) {
@@ -79,6 +89,9 @@ exports.ICNS = {
     calculate(input) {
         const inputLength = input.length;
         const fileLength = (0, utils_1.readUInt32BE)(input, FILE_LENGTH_OFFSET);
+        if (fileLength < SIZE_HEADER || fileLength > inputLength) {
+            throw new TypeError('Invalid ICNS file length');
+        }
         let imageOffset = SIZE_HEADER;
         let imageHeader = readImageHeader(input, imageOffset);
         let imageSize = getImageSize(imageHeader[0]);
diff --git a/dist/types/utils.js b/dist/types/utils.js
index 5224bbafe87551ac415cb3de234820ccc0ff6e2c..e66bc64a2bf54e2f484ce07cd94296f15b320b9d 100644
--- a/dist/types/utils.js
+++ b/dist/types/utils.js
@@ -49,9 +49,14 @@ function readUInt(input, bits, offset, isBigEndian) {
 }
 exports.readUInt = readUInt;
 function readBox(input, offset) {
-    if (input.length - offset < 4)
+    if (input.length - offset < 8)
         return;
     const boxSize = (0, exports.readUInt32BE)(input, offset);
+    // ISO BMFF boxes smaller than their 8-byte header cannot make forward
+    // progress. Reject zero/truncated sizes instead of returning a box that
+    // can trap JXL/HEIF callers in an infinite loop (CVE-2025-71329).
+    if (boxSize < 8)
+        return;
     if (input.length - offset < boxSize)
         return;
     return {
@@ -67,9 +72,7 @@ function findBox(input, boxName, offset) {
             break;
         if (box.name === boxName)
             return box;
-        // Fix the infinite loop by ensuring offset always increases
-        // If box.size is 0, advance by at least 8 bytes (the size of the box header)
-        offset += box.size > 0 ? box.size : 8;
+        offset += box.size;
     }
 }
 exports.findBox = findBox;
