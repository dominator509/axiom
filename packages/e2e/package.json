{
  "description": "End-to-end tests for the AXIOM dashboard + API against an ephemeral local stack (Playwright)",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.0"
  },
  "name": "@axiom/e2e",
  "private": true,
  "scripts": {
    "lint": "eslint tests/ *.ts",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "version": "0.1.0"
}
