{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", ".next-rehearsal/**"],
      "env": ["API_ORIGIN", "AXIOM_ISOLATED_VALIDATION"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {},
    "test": {
      "dependsOn": ["build"],
      "env": ["DATABASE_URL", "TEST_DATABASE_URL", "AXIOM_ISOLATED_VALIDATION"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false
    }
  }
}
